@using Microsoft.AspNetCore.Identity
@using CRM.Data
@inject SignInManager<CRM.Data.ApplicationUser> SignInManager
@inject UserManager<CRM.Data.ApplicationUser> UserManager
@{
   //var Categories = _context.Categories.Select(e => new { e.CategoryId, e.CategoryTlar });
}


@*@model CRM.ViewModels.Users

<div class="modal fade" id="AddModal" tabindex="-1" aria-labelledby="AddModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">

            <div class="modal-header bg-light p-3">
                <h5 class="modal-title" id="exampleModalLabel">@sharedResource["Add New Item"]</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close" id="close-modal"></button>
            </div>

            <form novalidate="true" asp-area="Admin" asp-page="/Users/Index" asp-page-handler="AddUser" enctype="multipart/form-data" onsubmit="return validateMyForm();">
                <div class="modal-body">
                    <input type="hidden" id="id-field" />
                    <div class="row g-3">
                        

                        <div class="col-lg-6">
                            <div>
                                <label for="@Model.FullName" class="form-label">@(sharedResource["FullName"])</label>
                                <input type="text" asp-for="@Model.FullName" class="form-control" placeholder="@sharedResource["Enter FullName"]" required />
                                <span asp-validation-for="@Model.FullName" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="col-lg-6">
                            <div>
                                <label for="@Model.Email" class="form-label">@sharedResource["Email"]</label>
                                <input type="email" asp-for="@Model.Email" class="form-control" placeholder="@sharedResource["Enter Email"]" required />
                                <span asp-validation-for="@Model.Email" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="col-lg-6">
                            <div>
                                <label for="@Model.PhoneNumber" class="form-label">@sharedResource["PhoneNumber"]</label>
                                <input type="text" data-val="true" data-val-required="@sharedResource["Please enter PhoneNumber"]" asp-for="@Model.PhoneNumber" class="form-control" placeholder="@sharedResource["Enter PhoneNumber"]" required />
                                <span asp-validation-for="@Model.PhoneNumber" class="text-danger"></span>
                            </div>
                        </div>

                        
                        <div class="col-lg-6">
                            <div>
                                <label for="@Model.Password" class="form-label">@sharedResource["Password"]</label>
                                <input type="password" data-val="true" data-val-required="@sharedResource["Please enter Password"]" asp-for="@Model.Password" class="form-control" placeholder="@sharedResource["Enter Password"]" required />
                                <span asp-validation-for="@Model.Password" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="col-lg-12">
                                <label asp-for="@Model.CategoryId" for="car-rental-name">@sharedResource["Category"] <small class="text-danger">*</small></label>

                            <select data-val="true" data-val-required="@sharedResource["Please choose a country"]" id="catId" asp-for="@Model.CategoryId" class="form-select required" aria-label="Default select example" required>
                                    <option selected value="Select Type" >@sharedResource["Select Category"]</option>
                                   
                                    @{
                                             if(@Categories!=null){
                                            foreach (var item in @Categories)

                                            {

                                                <option value="@item.CategoryId">@item.CategoryTlar</option>

                                            }
                                       }
                                    }

                            </select>

                            <span asp-validation-for="@Model.CategoryId" class="text-alert"></span>
                         </div>

                           


                    </div>
                 
                </div>
                <div class="modal-footer">
                    <div class="hstack gap-2 justify-content-end">
                        <button type="button" class="btn btn-light" data-bs-dismiss="modal">@sharedResource["Close"]</button>
                        <button type="submit" asp-page="Index" asp-page-handler="AddItem" class="btn btn-success" id="add-btn">@sharedResource["Add"]</button>
                    </div>
                </div>
            </form>

        </div>
    </div>
</div>*@